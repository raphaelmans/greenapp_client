name: address_repository
description: Address repository for greenapp.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^2.4.0
  equatable: ^2.0.3
  firebase_core: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.17.5
