name: authentication_repository
description: authentication domain of greenapp

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  equatable: ^2.0.3
  json_annotation: ^4.0.1
  authentication_api: 
    path: ../authentication_api


dev_dependencies:
  build_runner: ^1.10.0
  json_serializable: ^4.0.0


