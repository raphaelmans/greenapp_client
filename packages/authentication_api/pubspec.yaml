name: authentication_api
description: authentication api of greenapp

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  greenapp_api:
    path: ../greenapp_api
  json_annotation: ^4.0.1

dev_dependencies:
  build_runner: ^1.10.0
  json_serializable: ^4.0.0